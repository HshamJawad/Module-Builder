# تصدير HTML — سجل التغييرات ودليل الدمج

## الملفات

```
index.html                  ← معدَّل  (زر + وسما <script>)
src/module_model.js         ← جديد
src/exports_html.js         ← جديد
```

**لم يُمَسّ أي ملف آخر.** لا `exports_docx.js`، ولا `persistence.js`، ولا
`mb-translations.js`، ولا ورقة الأنماط.

مرفق كذلك ملفان تجريبيان — `SAMPLE_export_arabic.html` و
`SAMPLE_export_english.html` — افتحهما بنقرة مزدوجة قبل أن ترفع شيئاً.

---

## المعمارية: طبقتان لا واحدة

الطلب كان مُصدِّراً واحداً، والتنفيذ ملفان. السبب أن `exports_docx.js`
يؤدّي وظيفتين مندمجتين: يقرّر **ما الذي يدخل المستند**، ويبني كائنات
docx. والأولى ليست شأناً خاصاً بـ Word:

- ناتج تعلّم بلا أوراق معنونة يُتخطّى.
- ورقة بلا عنوان تُتخطّى.
- نموذج تقييم لم يملأه أحد يُتخطّى.
- صف غلاف بلا قيمة يُتخطّى.

هذه حقائق عن **الوحدة**، ولو خالفها مُصدِّر HTML لأنتج وحدة مختلفة عن
ملف Word — فتبدو الأداة غير موثوقة.

لذلك:

| الملف | مسؤوليته |
|---|---|
| `module_model.js` | يقرّر ماذا يدخل، ويسطّح اللغة، ويعيد **بيانات** فقط |
| `exports_html.js` | يستقبل النموذج ويرسمه |

**وهذا ما يجعل PPTX لاحقاً نصف عمل:** مُصدِّر الشرائح لن يحتاج قاعدة
جديدة واحدة، بل رسماً جديداً فقط. `module_model.js` لا يعرف شيئاً عن
DOM ولا docx ولا HTML، ويعيد كائناً قابلاً للتسلسل — فيُختبَر بقراءة
قيمته لا بفكّ ضغط ملف.

### مقايضة أعترف بها صراحةً

قواعد الإدراج في `module_model.js` **منقولة** من `exports_docx.js` لا
**مستخرَجة** منه. الاستخراج كان يعني تعديل مُصدِّر Word، وهو يُنتج اليوم
مخرَجه السابق بايتاً ببايت — ضمانة تساوي أكثر من تفادي تكرار شرط `if`.

الكلفة حقيقية: موضعان يرمّزان القاعدة نفسها وقد يفترقان. والعلاج اختبار
`agree_test` الذي يبني المشروع نفسه بالطريقتين ويتحقّق أنهما يتّفقان على
ما تحويه الوحدة. إن سقط يوماً، فقد افترقا، والإصلاح هو التوحيد لا ترقيع
أحد الطرفين.

---

## ما يفعله المخرَج

ملف `.html` **واحد** مستقل. الصور موجودة أصلاً في الحالة كـ data URLs
فتدخل كما هي، وCSS وJS مضمّنان. لا مجلد أصول، ولا خادم، ولا إنترنت.
نقرة مزدوجة يفتح، ومرفق ببريد يفتح.

- **فهرس جانبي لاصق** يبرز موضعك أثناء القراءة.
- **بحث** يصفّي الفهرس لا الصفحة — إخفاء الأقسام على قارئ منهمك مربك.
- **مفتاح الإجابات مخفي** حتى يطلبه المتدرّب. على الورق المفتاح هو
  الصفحة التالية، والجميع يسترق النظر.
- **قائمة التحقّق قابلة للتأشير**، والعلامات تبقى في متصفّح القارئ.
- **الطباعة**: تنسيق مستقل يخفي الفهرس ويُظهر الإجابات ويمنع قطع
  الأقسام.

العربية RTL بخاصية `dir` واحدة على `<html>`؛ ورقة الأنماط محايدة الاتجاه
بالكامل (`margin-inline`, `border-inline-start`, `text-align:start`).
هذا هو الموضع الذي يكون فيه HTML أسهل من docx بفارق كبير.

الكلفة: الحجم. وحدة بعشرين صورة تُنتج ملفاً كبيراً، لأن base64 أكبر
بثلث من البايتات التي يرمّزها. هذا ثمن «ملف واحد يعمل دائماً».

---

## نقاط تنفيذية

**الهروب من الترميز.** كل نص من المؤلف يمرّ عبر `_hxEsc`. وحدة تدريبية
عن HTML كانت ستغلق وسومها بنفسها، و`<` شاردة في معادلة كانت ستبتلع بقية
الفقرة بصمت.

**فواصل الأسطر.** الهدف والتحقّق الذاتي يُكتبان قوائمَ. الهروب أولاً ثم
تحويل الأسطر إلى `<br>` — بهذا الترتيب لا العكس.

**التخزين مغلَّف.** صفحة `file://` ترمي استثناءً عند `localStorage` في
بعض المتصفّحات، وحزمة تدريبية يجب ألّا تنهار لأن مربّع تأشير عجز عن
تذكّر نفسه.

**تسمية الزر** تتبع لغة **الواجهة** لا لغة التصدير — فهي تسمية على شريط
أدوات الأداة. والدالة `_hxLang()` تجيب عن سؤال آخر: بأي لغة يخرج الملف.

**الزر ليس `.tab`** — لنفس سبب زر الإعدادات: `switchTab()` كان سيمحو
القسم النشط.

**الملاحظات والجداول والصور** تُصدَّر بثيماتها وألوانها الأصلية من
`MARK_TYPES`، ونصّ الملاحظة يُترجم عبر `i18n` لأن `type.label` مفتاح
ترجمة لا عنوان.

---

## الاختبارات

سويتان جديدتان وواحدة للحراسة، إضافة إلى القديمة كلها:

```
module model      32/32   قواعد الإدراج، التسطيح، البوّابات، العربية
html output       79/79   الثلاث لغات: البنية، الروابط، التفاعل، الهروب
html button       14/14   الزر الحقيقي عبر events.js، حتى اسم الملف
docx/html agree    9/9    المُصدِّران يتّفقان على محتوى الوحدة
phase-1           35/35
word settings     59/59
sidebar fold      15/15
error handler     27/27
```

**والأهم:** مقارنة `word/document.xml` بالكود قبل هذا العمل وبعده:

```
en / fr / ar  →  identical raw: True
```

التصدير إلى Word لم يتغيّر بايتاً واحداً.

---

## الخطوة التالية

الدليل الترويجي لم يُحدَّث بعد ليشمل هذه الميزة — أُضيف له قسماً متى
أردت. وPPTX صار أقرب: `module_model.js` جاهز، والباقي رسم شرائح +
`pptx_bidi.js` على غرار `docx_bidi.js`.
