# Module Builder — حالة العمل

**المستودع:** `hshamjawad/Module-Builder` · GitHub Pages · البناء `2.0.1`
**التاريخ:** ٢٢ آب/أغسطس ٢٠٢٦

---

## ١. ما أُنجز في هذه الجلسة

### الميزة أ — الأقسام النصية الحرة ✅ مرفوعة وتعمل

زرّان يضيفان مربعات (عنوان + نص) اختيارية، لا تُصدَّر إن كان نصّها فارغاً:
- **تبويب صفحات التقديم:** «➕ إضافة قسم تقديمي». كل قسم يبدأ صفحة جديدة في الـDOCX. العنوان الأول مزروع بـ«محتويات الوحدة التدريبية».
- **بطاقة إدارة محصلات التعلم:** «➕ إضافة قسم»، قبل بطاقة معايير الأداء، مع `datalist` يقترح (محتويات محصلة التعلم / معايير التقييم / طرق التقييم / شروط التدريب). لا يظهر إلا بعد اختيار محصلة.

**ملف جديد:** `src/blocks.js`
**ملفات معدَّلة:** `index.html`, `mb-styles.css`, `mb_state.js` (`introBlocks: []`), `storage.js`, `outcomes.js` (`blocks: []` في كل محصلة), `exports_docx.js`, `mb-translations.js`

قرارات مقصودة:
- الحقول تكتب في الحالة **عند كل ضغطة مفتاح**، لا عند الحفظ — حتى لا تضيع جهة من الزوج ثنائي اللغة عند تبديل لغة المحتوى.
- العنوان الافتراضي يُزرع في **الجهتين معاً** عند الإنشاء (نمط `mbSeedCoverLabels`)، لا كمفتاح i18n.
- أقسام المحصلة تعيش في `lo.blocks` داخل كائن المحصلة، فتُحفظ مع `modulesData` بلا سطر في `storage.js`.

### الميزة ب — تصغير الصور عند الرفع ✅ مرفوعة ومقيسة

**ملف جديد:** `src/image_prep.js` — ثلاثة ملامح:

| الملمح | السقف | الصيغة |
|---|---|---|
| `cover` | 2480 × 3508 (A4 @300dpi) | JPEG q0.85 |
| `content` | 1600 × 1600 | JPEG q0.82 |
| `qr` | 800 × 800 | PNG (بلا إعادة ترميز) |

**القياس الفعلي:** غلاف ١٨ ميغا ← **٤٩٥ ك.ب**.

**ملفات معدَّلة:** `cover_images.js` (الأربعة صارت `_mbSetCoverImage`), `content.js` (صور المحتوى + QR + صورة المعلومات), `steps.js` (`handleStepImageUpload` جديد؛ `data-act` في الوسم أُعيدت تسميته من `handleImageUpload`), `index.html`, `mb-translations.js` (`dgImageOptimized`)

قرارات مقصودة:
- **JPEG لا WebP** — الصور تنتهي في DOCX، ووورد القديم لا يعرض WebP.
- **QR لا يُعاد ترميزه** — تموّج JPEG حول مربعات التموضع مسحٌ فاشل.
- `createImageBitmap` بـ`imageOrientation:'from-image'` — وإلا تُحفظ صور الهواتف مقلوبة نهائياً.
- كل مسار فشل يعيد **الأصل**. لا حالة ينتهي فيها المستخدم بلا صورة.

### الميزة ج — IndexedDB للتخزين المحلي ✅ مرفوعة ومختبرة

**ملفان فقط:** `persistence.js` (طبقة IDB + ارتداد + ترحيل)، `autosave.js` (`tryRestore` صار غير متزامن).

**القياس الفعلي:** الحصة من ٥ ميغا إلى **١٠ غيغا** (`mbStorageEstimate()`), و`mbSaveDoc/mbLoadDoc` عملا في المتصفح.

قرارات مقصودة:
- **التفضيلات تبقى في `localStorage`** (`mb_content_lang`, `dacum_lang`…) — تُقرأ أثناء أول رسم، ولا قراءة متزامنة في IndexedDB إطلاقاً.
- **مؤقّت ٣ ثوانٍ على `indexedDB.open()`** — سفاري الخاص لا يستجيب ولا يفشل؛ بلا المؤقّت لا تُقلع الأداة.
- **الترحيل مرة واحدة:** أول فتح ناجح ينقل النسخة من `localStorage` ثم يمسحها.
- **لا `JSON.stringify` في مسار الحفظ** — structured clone.

### إصلاحات جانبية في `autosave.js` ✅ مرفوعة

- خمسة حقول كانت تُقرأ من `.value` (جهة واحدة من الزوج) صارت تُقرأ من `mbState` بعد `syncProjectTextFromDOM()`. كانت النسخة التلقائية تحفظ نصف مشروع.
- مسار الاستعادة يكتب في الحالة ثم يرسم عبر `applyProjectTextToDOM()` (كان `.value` مباشرة ⇒ `[object Object]` بعد التحويل إلى أزواج).
- `|| 'References'` أُزيل من الموضعين — كان يكتب فوق العنوان العربي المزروع.

---

## ٢. مفتوح ولم يُنفَّذ

1. **دورة الحفظ/الاستعادة لم تُختبر.** (اكتب ← انتظر وميض النقطة ← Ctrl+R). هذا الاختبار الوحيد الذي يمرّ على `tryRestore` غير المتزامن. **افعله.**
2. **خطأ `addContentSection is not defined`** ظهر مرة في `app.js:36`، ثم أثبت الفحص أن الدالة موجودة. غالباً من نسخة صفحة قديمة. لم يتكرر.
3. **فصل الصور إلى سجلات مستقلة (كانت «المرحلة ب» في نقاشنا)** — **مؤجَّلة عمداً.** مبرّراها سقطا: الحصة زالت، و`JSON.stringify` خرج من مسار الحفظ. تكلفتها ١١٣ موضعاً في ٩ ملفات. تُنفَّذ فقط إذا ظهر **بطء محسوس** عند الكتابة في وحدة كبيرة.
4. **زر إعادة معالجة صور الوحدات القديمة** — عُرض ولم يُطلب. المعالجة تعمل عند الرفع فقط؛ الصور في ملفات `.json` المحفوظة سابقاً ما تزال بأحجامها الأصلية.

---

## ٣. معمارية يجب معرفتها قبل أي تعديل

- **سكربتات تقليدية، لا ES modules.** `var` في المستوى الأعلى يضع الاسم على `window`، وعليه يعتمد مُرسِل الأحداث.
- **`events.js`** يفوّض من `document` عبر `data-act` + `data-args` (JSON). يحلّ الاسم من `MB_UI_ACTIONS` ثم من `window[name]` — **فالدوال الجديدة لا تحتاج تسجيلاً**. يستدعي `fn.apply(el, args.concat([el, ev]))`، فأي معامل أول اختياري قد يستقبل العنصر نفسه.
- **ثنائية اللغة (Schema v4):** كل ما يكتبه المستخدم زوج `{en, ar}`. `bilang.js` يملك `biGet/biGetStrict/biPut/biNew/biUpgrade`. ثلاثة مفاتيح مستقلة: لغة الواجهة، لغة المحتوى، لغة التصدير.
- **`biFlattenDeep`** يسطّح الحالة كلها إلى لغة واحدة في أول `exportToDocx` — فداخل التصدير كل القيم نصوص عادية.
- **ترتيب التحميل مهم:** `export_ui.js` (818) بعد `steps.js` (816). أي تعريف مكرَّر في الأول يظلّل الثاني.
- **مواضع الصور:** `mbState.stepImages` (منسوخة أيضاً داخل `activity.images` في `sheets.js`)، `contentSectionImages`، `frontCoverImage`/`backCoverImage`، `infoQRImage`/`activityQRImage`.

---

## ٤. ما يحتاجه المساعد في المحادثة الجديدة

الملفات التي رآها بالكامل (٢٧ ملفاً) — أرسل منها فقط ما يمسّه التعديل الجديد:

`index.html` · `mb-styles.css` · `error-handler2.js` · `version.js/json/yml`
`src/`: `persistence.js` `mb-translations.js` `mb_state.js` `bilang.js` `uid.js` `image_prep.js` `dialog.js` `ui.js` `tabs.js` `covers.js` `cover_images.js` `workteam.js` `modules.js` `outcomes.js` `blocks.js` `sheets.js` `content.js` `criteria.js` `references.js` `resources.js` `marks.js` `steps.js` `storage.js` `export_ui.js` `exports_docx.js` `contentlang_ui.js` `events.js` `app.js` `autosave.js` `docx_bidi.js` `assessment.js`

**درسان من هذه الجلسة يوفّران وقتاً:**
- لا تفترض أن دالة غائبة لأنها غابت عن الملفات المرسلة. تحقّق أولاً بـ`typeof window.X` في الـConsole. (حدث مرتين هنا، وكاد يُكتب بديل يظلّل الأصل.)
- سلّم **ملفات مطبَّقة جاهزة للرفع**، لا وثائق رقع. الرقعة الأولى لم تُطبَّق فلم يظهر الزر، وضاعت جولة كاملة.
